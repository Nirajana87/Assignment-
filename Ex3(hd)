#include<stdio.h>
int main()
{
int num;
long int factorial=1;
printf("enter a number:");
scanf("%d",&num);
if (num<0)
{
printf("factorial of a negative number don't exist",\n)
}else}
for(int i=1;i<=num;i++)
{
factorial*=i;
}printf("factorial of %d is %d \n",num,factorial);
}
}
